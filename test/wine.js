// Generated by CoffeeScript 1.4.0
(function() {
  var nearestNeighbor, should;

  should = require('should');

  nearestNeighbor = require('../lib/main');

  describe('K Nearest Neighbor', function() {
    return describe('with basic wine testCase', function() {
      var testCase;
      testCase = require('./test_case_simple');
      it('should return the nearest neighbor', function() {
        nearestNeighbor(testCase.tasteProfile1, testCase.wineList)[0].label.should.eql('C');
        nearestNeighbor(testCase.tasteProfile2, testCase.wineList)[0].label.should.eql('E');
        return nearestNeighbor(testCase.tasteProfile3, testCase.wineList)[0].label.should.eql('J');
      });
      it('should return 5 nearest neighbors, sorted by distance');
      return it('should return all of Y sorted by distance if Y.length < 5');
    });
  });

}).call(this);
